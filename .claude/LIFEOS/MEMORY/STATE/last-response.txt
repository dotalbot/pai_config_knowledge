════ LifeOS ═══════════════════════════

Scheduled durably, not in session memory. Session cron would have died when this conversation ends, which is exactly the wrong mechanism for a seven-day question.

`ReflectionCorpusCheck.sh` fires from your real crontab at 09:23 on Thursday the 18th. It compares against today's baseline of 8 entries and 1 schema-9, then writes a verdict to `MEMORY/STATE/reflection-corpus-check.md`: **working** at 5+ new, **partial** at 1 to 4, **failed** at zero.

The failure branch says plainly that zero new entries means the gate is not firing as designed, and that this *is* the finding — with an explicit instruction not to paper over it with a fourth workaround. Today's dry run correctly reports +0 against its own baseline, which is how I know the arithmetic works.

It's also recorded in `PROJECTS.md` as a dated open ask, so it surfaces conversationally rather than only in a log file nobody opens.

🔧 CHANGE:

- `ReflectionCorpusCheck.sh` + crontab entry · `PROJECTS.md` open ask · pushed

✅ VERIFY:

- Dry run produces the correct verdict; crontab entry confirmed installed

🗣️ JellyPai: I'll raise it on the 18th either way, including if the answer is that it didn't work.