373 notes — my `fd` count was wrong (depth/ignore behaviour). The vault is substantial. I now have all three sources mapped, so here's the proposal.

════ LifeOS ═══════════════════════════

I found your real setup, and it's better than expected. Three live sources, no new tracker needed.

**JellyDesk Hub on `jellyberry:8000`** exposes `/api/v1/dashboard` — 8 Hermes boards with column counts, schema-versioned with a revision hash. `3dprint-loader` sits alongside `logk-video-ingest`, so physical and code projects already share one system. That's the integration point, and it's clean.

**GitHub** gives 13 repos pushed in 30 days via `gh`, already authenticated here.

**Obsidian `Projects/`** holds 373 notes across 8 folders, matching board names closely: Logk, ConveyorDesk, Homelab, 3D Printing.

**What I'd build:** a Pulse `/projects` tab that joins those three on project identity. One card per project showing board state, last commit, and vault notes — with the insight being *divergence*. A board with 29 todo and no commits in three weeks is stalled. A repo pushed daily with no board is untracked. Physical projects have no repo, so they're judged on board plus notes alone.

Two things to know. The existing `/projects` module errors because it expects a deploy-oriented markdown table, so I'd replace its source rather than patch it. And `active_work` is empty with all boards at `active=0`, so I'd verify what "active" means in your Hermes before leaning on it.

🗣️ JellyPai: Want me to write this up as a proper plan before touching code?