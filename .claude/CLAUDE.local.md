# CLAUDE.local.md — Dom-specific personal overrides

This file is gitignored. Use it for local machine overrides that should not be
committed to the shared ~/.claude repo (e.g., machine-specific paths, temp rules
for an active experiment, dev-only tool preferences).

Merge order: CLAUDE.md is loaded first; this file's rules override it where they conflict.

## Local Overrides

<!-- Add machine-specific rules here -->
