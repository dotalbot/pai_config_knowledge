**🔄 RE-READ:**
🔄 "please go and fix": ✓ addressed — both CRITICAL bugs fixed and verified live.

**📦 DELIVERABLE COMPLIANCE:** D1 (fix GetTranscript.ts) — ✓ shipped, verified with real transcript extraction. D2 (fix Advisor) — ✓ shipped, verified with live advisor call returning real content.

━━━ 📃 SUMMARY ━━━ 7/7

📃 CONTENT:
- `PAI/TOOLS/GetTranscript.ts` — rewritten to use yt-dlp's own `--write-auto-sub --write-sub` caption download instead of the missing `fabric` CLI, with VTT→plaintext conversion and graceful no-captions/private-video handling. Verified: extracted a real 217-char transcript from a live video.
- `PAI/TOOLS/Inference.ts` — root cause was `claude-fable-5` reporting "currently unavailable" on **stdout** while the error path only checked **stderr**, producing the generic swallowed message. Added `runClaudeCli()` helper + `isModelUnavailable()` detection + one-shot retry against `claude-opus-4-8` (mirrors settings.json's existing `fallbackModel` chain). Verified: live advisor call now returns real content instead of a bare error.

🖊️ STORY:
- Problem: two CRITICAL bugs from the prior audit — broken YouTube transcripts, silently-dead Advisor
- What we did: root-caused both (missing binary, stdout/stderr mixup + unavailable model), fixed in place, no new dependencies added
- How it went: both verified live against real inputs, including the advisor itself confirming the fix works and correctly flagging that this needed its own ISA
- What's next: nothing blocking — the other findings (TELOS, PROJECTS.md, counters) remain as separate optional follow-ups from the original report

🗣️ JellyPai: Both fixed and proven live — transcripts pull real captions now, and Advisor actually answers instead of dying silently.